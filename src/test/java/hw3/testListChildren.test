# test if we can corectly listChildren for a Node in the graph.
# list single children
# Create a graph
CreateGraph graph1

# Add a pair of nodes
AddNode graph1 ValidNode1
AddNode graph1 ValidNode2

# Add an edge
AddEdge graph1 ValidNode1 ValidNode2 Edge1

# Print outgoing edges from ValidNode1
ListChildren graph1 ValidNode1


# test if we can corectly listChildren for a Node in the graph.
# list multiple children
# Create a graph
CreateGraph graph1

# Add a pair of nodes
AddNode graph1 ValidNode1
AddNode graph1 ValidNode2

# Add an edge
AddEdge graph1 ValidNode1 ValidNode2 Edge1

# Print outgoing edges from ValidNode1
ListChildren graph1 ValidNode1